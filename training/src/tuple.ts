/**
 * 튜플
 * 길이와 타입이 고정된 배열
 */

let strTuple: [string, string];
strTuple = ['a' , 'b'];

let numTuple: [string, number] = ['a', 1];

let unionTuple: [string, number][] = [
    ['a', 1],
    ['b', 2]
]

